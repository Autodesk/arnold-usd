Incorrect number of motion keys with skinned primvar normals and motion blur

Fixes #1443

author: cyril.pichard@autodesk.com 

PARAMS: {'scene': 'test.usda', 'kick_params': '-frame 3', 'hydra': False }

# Disabled in hydra as 1) the amount of keys is clamped in hydra, 2) we receive twice the same key value through hydra