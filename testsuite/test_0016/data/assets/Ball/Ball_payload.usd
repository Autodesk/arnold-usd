#usda 1.0
(
    defaultPrim = "Ball"
    upAxis = "Z"
)

def "Ball" (
    assetInfo = {
        asset identifier = @./assets/Ball/Ball.usd@
        string name = "Ball"
    }
    add references = @./Ball.geom.usd@</Ball>
)
{
}


